import styled from "@emotion/styled";
import { Box } from "@mui/system";

export const AllArticlesContainerStyled = styled(Box)({
  display: "flex",
  justifyContent: "flex-end",
  marginRight: 16,
});
